import { getCountries } from '../../helpers/getCountries';

describe('Pruebas getCountries', () => {

    test('Numero de registros debe ser 10', async () => {
        //const countries = await getCountries();
        //expect(countries.length).toBe(10);
    });

});
